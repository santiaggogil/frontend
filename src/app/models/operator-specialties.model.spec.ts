import { OperatorSpecialties } from './operator-specialties.model';

describe('OperatorSpecialties', () => {
  it('should create an instance', () => {
    expect(new OperatorSpecialties()).toBeTruthy();
  });
});
